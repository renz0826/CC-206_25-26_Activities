import 'package:flutter/material.dart';

class Pet extends StatelessWidget {
  const Pet({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    body: Container(child: Center(child: Text("Pet"))),
  );
}
